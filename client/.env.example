# Optional: API base URL when frontend is served from a different origin (e.g. dev server on port 5173).
# If not set, the app uses relative /api/v1 (same origin).
# VITE_API_BASE_URL=http://localhost:3000/api/v1
